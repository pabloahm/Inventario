<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="inventario.list">
      <field name="name">inventario list</field>
      <field name="model">inventario.inventario</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="inventario.action_window">
      <field name="name">inventario window</field>
      <field name="res_model">inventario.inventario</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="inventario.action_server">
      <field name="name">inventario server</field>
      <field name="model_id" ref="model_inventario_inventario"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="inventario" id="inventario.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="inventario.menu_1" parent="inventario.menu_root"/>
    <menuitem name="Menu 2" id="inventario.menu_2" parent="inventario.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="inventario.menu_1_list" parent="inventario.menu_1"
              action="inventario.action_window"/>
    <menuitem name="Server to list" id="inventario" parent="inventario.menu_2"
              action="inventario.action_server"/>
    -->
  </data>
</odoo>